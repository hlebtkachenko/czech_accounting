# Placeholders only. Never commit real values.
# The real secrets live ONLY on the VPS at ~/frappe/frappe.env (chmod 600) and in a
# password manager. This file documents which variables the dev stack expects.

MARIADB_ROOT_PASSWORD=CHANGE_ME
ADMIN_PASSWORD=CHANGE_ME
